{
  "name": "canton-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "esbuild src/app.js --bundle --format=esm --target=es2022 --outfile=dist/app.js && cp public/* dist/"
  },
  "dependencies": {
    "keycloak-js": "26.1.0"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  }
}
